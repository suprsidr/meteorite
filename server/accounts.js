/*Accounts.onCreateUser(function(options, user) {
  user.intercomHash = IntercomHash(user, '8389a1e3289e4dfcbf887bb817969d34f9df3548');

  if (options.profile)
    user.profile = options.profile;

  return user;
});*/